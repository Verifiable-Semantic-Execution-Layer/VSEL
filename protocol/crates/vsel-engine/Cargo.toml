[package]
name = "vsel-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
vsel-core = { path = "../vsel-core" }
vsel-invariants = { path = "../vsel-invariants" }
serde = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
